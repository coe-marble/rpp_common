# rpp_common

Common Python interfaces shared by RPP projects.

This package provides:

- `RPP_Plugin`
- `ParameterDescription` / `ParamDescriptor`
- `LogEntry`
- `RegistryInfo`
- `IOArgument`
- `DataModel`

Install in editable mode from this folder:

```bash
pip install -e .
```